┌─────────────────────────────────────────────────────────────────┐
│           GIT SYNC - CLEANUP & LOG ROTATION                     │
│                    QUICK REFERENCE                              │
└─────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════
📦 WHAT CHANGED
═══════════════════════════════════════════════════════════════════

✅ Auto-cleanup of ~/sync-workspace/ after each sync
✅ Log rotation (7 days retention)
✅ 50% disk space savings
✅ Better SD card longevity

═══════════════════════════════════════════════════════════════════
🚀 QUICK SETUP
═══════════════════════════════════════════════════════════════════

1. Update script:
   sudo cp sync-with-cleanup.py /opt/git-sync/sync.py
   sudo nano /opt/git-sync/sync.py  # Update username

2. Setup log rotation:
   chmod +x setup-log-rotation.sh
   ./setup-log-rotation.sh

3. Test:
   export GITHUB_TOKEN="your_token"
   python3 /opt/git-sync/sync.py

4. Restart service:
   sudo systemctl restart git-sync.service

═══════════════════════════════════════════════════════════════════
💾 DISK SPACE BEFORE/AFTER
═══════════════════════════════════════════════════════════════════

BEFORE:
  ~/repositories/     2.5 GB  ← Git server
  ~/sync-workspace/   2.5 GB  ← Sync mirror
  ─────────────────────────
  Total:              5.0 GB

AFTER:
  ~/repositories/     2.5 GB  ← Git server
  ~/sync-workspace/   0 MB    ← Deleted!
  ─────────────────────────
  Total:              2.5 GB  💾 Saved 2.5 GB!

═══════════════════════════════════════════════════════════════════
🔧 CONFIGURATION
═══════════════════════════════════════════════════════════════════

Enable/Disable Cleanup:
  Edit /opt/git-sync/sync.py:
  "cleanup_workspace": True/False

Change Log Retention:
  Edit /opt/git-sync/sync.py:
  "log_retention_days": 7  # Change to 14, 30, etc.

Manual Log Cleanup:
  sudo journalctl --vacuum-time=7d
  sudo journalctl --vacuum-size=100M

═══════════════════════════════════════════════════════════════════
📊 MONITORING
═══════════════════════════════════════════════════════════════════

Check disk usage:
  du -sh ~/repositories/

Verify workspace cleaned:
  ls ~/sync-workspace/ 2>&1

Check log size:
  sudo journalctl --disk-usage
  du -sh ~/git-sync.log

View sync logs:
  sudo journalctl -u git-sync.service -n 50

═══════════════════════════════════════════════════════════════════
⚡ PERFORMANCE
═══════════════════════════════════════════════════════════════════

First sync after cleanup:
  Time: 2-30 minutes (needs to re-clone)
  Trade-off: Worth it for 50% space savings

Subsequent syncs:
  Time: 30 seconds - 2 minutes (only fetches changes)
  Frequency: Every hour (by default)

═══════════════════════════════════════════════════════════════════
🔍 VERIFICATION
═══════════════════════════════════════════════════════════════════

1. Run sync:
   sudo systemctl start git-sync.service

2. Check logs for cleanup message:
   sudo journalctl -u git-sync.service | grep -i cleanup

   Should see:
   "Cleaning up sync workspace..."
   "✓ Cleaned up workspace (freed X MB)"

3. Verify workspace deleted:
   ls ~/sync-workspace/
   # Should not exist

4. Check disk savings:
   du -sh ~/repositories/

═══════════════════════════════════════════════════════════════════
🆘 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════

Workspace not deleted:
  Check permissions: ls -la ~/
  Check logs: journalctl -u git-sync.service -n 100

Sync too slow:
  Increase timeout in /etc/systemd/system/git-sync.service:
  TimeoutStartSec=1200

Logs still growing:
  Verify rotation: cat /etc/systemd/journald.conf.d/git-sync.conf
  Manual cleanup: sudo journalctl --vacuum-time=7d

Out of space:
  Emergency: rm -rf ~/sync-workspace/
  Clean logs: sudo journalctl --vacuum-size=50M

═══════════════════════════════════════════════════════════════════
↩️  ROLLBACK
═══════════════════════════════════════════════════════════════════

To disable cleanup:
  sudo nano /opt/git-sync/sync.py
  Change: "cleanup_workspace": False
  sudo systemctl restart git-sync.service

To restore old script:
  sudo cp /opt/git-sync/sync.py.backup /opt/git-sync/sync.py
  sudo systemctl restart git-sync.service

═══════════════════════════════════════════════════════════════════
✅ BENEFITS
═══════════════════════════════════════════════════════════════════

✓ 50% less disk space used
✓ Automatic log cleanup (7 days)
✓ Better SD card longevity
✓ Cleaner system
✓ Same reliability
⚠ Slightly slower syncs (acceptable)

═══════════════════════════════════════════════════════════════════
📞 SUPPORT
═══════════════════════════════════════════════════════════════════

Check logs:     sudo journalctl -u git-sync.service -f
Status:         git-sync-status.sh
Documentation:  CLEANUP-SETUP-GUIDE.md
GitHub:         github.com/jenishjain/git-sync

═══════════════════════════════════════════════════════════════════

💡 TIP: Print this card and keep it handy for quick reference!
